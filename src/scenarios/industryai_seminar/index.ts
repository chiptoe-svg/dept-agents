// Industry-AI-seminar scenario profile.
//
// Registers the seminar scenario (roles, personas, greetings, folder→role
// detection). When more seminar-specific behavior is needed (custom pair
// consumers, etc.), add modules here and import them below.

import './scenario.js';
